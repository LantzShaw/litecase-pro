import actionTypes from '../actions/actionTypes'

const initialState = {
  isLoading: false,
  list: [],
}

export default (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state
  }
}
